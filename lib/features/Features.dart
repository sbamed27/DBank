import 'package:flutter/material.dart';

class Features extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Awesome features', style: TextStyle(
          color: Colors.black,
          fontSize: 84,
          fontFamily: 'Gilroy Bold'
        ),),
      ],
    );
  }
}
